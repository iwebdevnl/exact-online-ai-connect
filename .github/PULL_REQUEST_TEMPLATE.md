## What changed

## Checklist
- [ ] JSON files still parse (server.json, plugin/marketplace JSON)
- [ ] README claims re-verified (no "every plan / any client", no SOC 2 / ISO 42001; ISO 27001 only)
- [ ] Docs build (`mkdocs build --strict`) if docs changed
